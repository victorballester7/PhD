\documentclass{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usetikzlibrary{patterns,decorations.markings,backgrounds}
\usetikzlibrary{arrows.meta}

\def\a{-10}
\def\d{4}
\def\w{15}
\def\b{35}
\def\scale{0.2}
\def\eps{0.5}
\def\wall{0.5}

\def\aa{\a*\scale}
\def\dd{\d*\scale}
\def\ww{\w*\scale}
\def\bb{\b*\scale}

\begin{document}
\begin{tikzpicture}[scale=1.2]

% Wall
\draw[thick] (\aa,0) -- (0,0) -- (0,-\dd) -- (\ww,-\dd) -- (\ww,0) -- (\bb,0);

\foreach \x in {\aa,...,0} {
    \draw[very thin] (\x,0) -- ++(225:0.4); % Wall hatching
}

% BL Profile (velocity profile)
\draw[thick] (0,0) -- (0,1.2);
\draw[-Stealth,thick] (0,0.2) -- (0.5,0.2);
\draw[-Stealth,thick] (0,0.5) -- (1.0,0.5);
\draw[-Stealth,thick] (0,0.8) -- (1.3,0.8);
\draw[-Stealth,thick] (0,1.1) -- (1.5,1.1);

% Labels
\node at (2,-0.3) {Wall};
\node at (1.2,1.4) {BL profile};

\end{tikzpicture}
% \begin{tikzpicture}[scale=1]
%     % fill the rectangle
%   \draw[draw=black, line width = 1.25pt] (\aa, 0) -- (0,0) -- (0, -\dd) -- (\ww, -\dd) -- (\ww, 0) -- (\bb, 0);
%   \draw[pattern=north east lines] (\aa,0) rectangle (0,-\wall);  
%     % arrows
%   % \draw[stealth-stealth, thick] (\aa-\eps, 0) -- (\aa-\eps, \hh) node[midway, left] {$h$};
%   % \draw[stealth-stealth, thick] (\aa, -\eps) -- (0, -\eps) node[midway, below] {$L_i$};
%   % \draw[stealth-stealth, thick] (\ww, -\eps) -- (\bb, -\eps) node[midway, below] {$L_o$};
%   % \draw[stealth-stealth, thick] (0, -2*\eps) -- (\ww, -2*\eps) node[midway, below] {$w$};

%   % %depth
%   % \def\xloc{2*\ww}
%   % \def\len{0.5}
%   % % node ate the end
%   % \draw[-stealth, thick] (\xloc, -\len-\dd) -- (\xloc, -\dd) node[above, anchor=west] {$d$};
%   % \draw[stealth-, thick] (\xloc, 0) -- (\xloc, \len);
% \end{tikzpicture}
\end{document}
