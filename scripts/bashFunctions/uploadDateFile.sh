# create a file with the date and time of the job submission
function uploadDateFile {
    # I comment this out in order to have a record of all execution dates
    # rm -f *.date 
    echo "" > $datefile
}
